---
apiVersion: v1
kind: Service
metadata:
  name: db-redis-metrics
  labels:
    app: db-redis
spec:
  type: ClusterIP
  ports:
    - name: metrics
      port: 9121
      targetPort: metrics
  selector:
    app: db-redis
