---
kind: NetworkPolicy
apiVersion: "networking.k8s.io/v1"
metadata:
  name: db-postgres-slaves
  labels:
    app: db-postgres
    role: slave
spec:
  podSelector:
    matchLabels:
      app: db-postgres
  ingress:
    # Allow inbound connections
    - ports:
        - port: 5432
          protocol: TCP
      from:
        - podSelector:
            matchLabels:
              db-postgres-client: "true"
    # Allow prometheus scrapes
    - ports:
        - port: 9187
  egress:
    - ports:
        - port: 5432
          protocol: TCP
      to:
        - podSelector:
            matchLabels:
              app: db-postgres
              role: master
