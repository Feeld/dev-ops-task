---
apiVersion: v1
kind: Service
metadata:
  name: api-redis-metrics
  labels:
    app: api-redis
spec:
  type: ClusterIP
  ports:
    - name: metrics
      port: 9121
      targetPort: metrics
  selector:
    app: api-redis
