---
apiVersion: v1
kind: Service
metadata:
  name: api-redis-headless
  labels:
    app: api-redis
spec:
  type: ClusterIP
  clusterIP: None
  ports:
    - name: redis
      port: 6379
      targetPort: redis
  selector:
    app: api-redis
