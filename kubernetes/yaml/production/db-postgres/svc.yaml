---
apiVersion: v1
kind: Service
metadata:
  name: db-postgres
  labels:
    app: db-postgres
spec:
  type: ClusterIP
  ports:
    - name: postgres
      port: 5432
      targetPort: postgres
  selector:
    app: db-postgres
    role: master
