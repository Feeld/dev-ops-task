---
apiVersion: v1
kind: Service
metadata:
  name: db-postgres-headless
  labels:
    app: db-postgres
spec:
  type: ClusterIP
  clusterIP: None
  ports:
    - name: postgres
      port: 5432
      targetPort: postgres
  selector:
    app: db-postgres
