---
apiVersion: v1
kind: Service
metadata:
  name: db-redis-master
  labels:
    app: db-redis
spec:
  type: ClusterIP
  ports:
    - name: redis
      port: 6379
      targetPort: redis
  selector:
    app: db-redis
    role: master
